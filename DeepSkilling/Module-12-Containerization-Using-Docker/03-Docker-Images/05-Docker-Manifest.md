# Docker Manifest

A Docker manifest is a JSON document that describes a Docker image.

It contains information such as:

- Image layers
- Architecture
- Operating system
- Image digest
- Tags

Docker uses the manifest to pull the correct image version.