# What is a Docker Image?

A Docker image is a read-only template containing the application code, runtime, libraries, dependencies, and configuration files required to create a Docker container.

## Features

- Read-only
- Portable
- Reusable
- Versioned
- Layered architecture