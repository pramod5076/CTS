# Container Layer

When a Docker image runs, Docker adds a writable container layer on top of the image.

All file changes made by the running container are stored in this layer.

The underlying image remains unchanged.