# Run a Container in Detached Mode

Detached mode allows a container to run in the background.

## Syntax

```bash
docker run -d IMAGE
```

## Example

```bash
docker run -d nginx
```

The terminal returns immediately while the container continues running in the background.