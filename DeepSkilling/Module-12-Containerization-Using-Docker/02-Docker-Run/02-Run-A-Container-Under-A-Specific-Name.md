# Run a Container Under a Specific Name

A custom name can be assigned to a container using the `--name` option.

## Syntax

```bash
docker run --name CONTAINER_NAME IMAGE
```

## Example

```bash
docker run --name my-nginx nginx
```

Using meaningful names makes it easier to identify and manage containers.