# Listing All Docker Networks

Use the following command to display all available Docker networks.

## Syntax

```bash
docker network ls
```

Example Output:

```text
NETWORK ID     NAME      DRIVER

abc123         bridge    bridge

def456         host      host

ghi789         none      null
```

This command displays the network ID, name, and network driver.